# DesignForge AI Generated Application

## 🚀 Quick Start

### Prerequisites
- Node.js 18+ (for frontend)
- Python 3.11+ (for backend)
- PostgreSQL 15+ (for database)
- Docker (optional, for containerized deployment)

### Setup

#### Frontend
```bash
cd frontend
npm install
npm run dev
# Runs on http://localhost:3000
```

#### Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
# Runs on http://localhost:8000
```

#### Database
```bash
# Run the schema.sql file in your PostgreSQL database
psql -U user -d designforge -f database/schema.sql
```

### Docker Deployment
```bash
docker-compose up --build
```

### Production Deployment

#### Frontend (Vercel)
```bash
vercel deploy
```

#### Backend (Railway)
```bash
# Connect your GitHub repo to Railway
# It will auto-deploy using railway.toml config
```

## 📁 Project Structure
```
.
├── frontend/          # Next.js 14 application
│   ├── page.tsx      # Main page component
│   ├── layout.tsx    # Root layout
│   └── globals.css   # Tailwind styles
├── backend/          # FastAPI application
│   ├── main.py       # API entry point
│   └── models.py     # Data models
├── database/         # PostgreSQL schema
│   └── schema.sql    # Database tables
└── docker-compose.yml # Container orchestration
```

## 🛠️ Tech Stack

- **Frontend**: Next.js 14, TypeScript, Tailwind CSS
- **Backend**: FastAPI, Python, Pydantic
- **Database**: PostgreSQL
- **Deployment**: Docker, Vercel, Railway

---

Generated by **DesignForge AI** 🎨✨
